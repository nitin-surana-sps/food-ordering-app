//Submit Button
let resButton = document.getElementById("submitButton");

//when clicked, redirects back to the index, plus calls datastore (to be added)
resButton.addEventListener("click", function() {
    // your code here...
  window.location.replace("index.html");
})
